# 2.3.0 工作区布局

镜像 `billcoann/xibao-server:2.3.0`，APK 使用 xibao-iptv v2.3.0 Release。群晖由管理员自行部署，保留原数据挂载。授权协议和已允许设备不变。

- 导航、顶部操作栏固定；列表在表格自身区域滚动，表头固定。
- 总览改为统计、快捷入口、源状态、配置进度和频道分布，去掉占空间的宣传区。
- 家庭设备改为保护状态条和单一授权列表，不重复展示相同设备和长说明。
- 压缩卡片间距与表格行高。频道分组过多时横向滚动，避免挤压列表。
- 在 1366×768、1280×720 检查常用页面的一屏布局；窄屏和低高度窗口仍允许工作区内部滚动，确保表单可访问。大量频道不可能一次全部展示，只滚动表格，不滚动整个页面。

后端默认 APK 链接升级到 2.3.0。管理员已手填的旧链接保持原值，需要自行更新。
