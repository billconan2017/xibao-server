# 喜宝 TV 2.2 家庭升级说明

后端镜像：`billcoann/xibao-server:2.2.0`。APK：GitHub xibao-iptv 的 v2.2.0 Release。
群晖由管理员自行更新容器。保留 `/app/data` 的数据挂载；更新前备份数据库。2.2 增加设备密钥摘要和节目频道名字段，原有直播源、设备记录与管理员账号保留。

## 家庭授权

1. 后端和 APK 一起升级到 2.2。旧 APK 没有设备密钥，不能使用新版心跳接口。
2. 旧数据库保留原来的授权开关。进入 **家庭设备 → 开启设备授权**。新安装默认开启。
3. APK 连接后会显示新的设备 ID。在家庭设备页核对 ID 和型号，点击 **允许观看**，APK 按刷新。无需配置套餐。
4. 每台安装生成随机密钥，后台只存摘要。升级至 2.2 首次生成新身份，需重新允许一次；之后覆盖安装保留身份。卸载、清除应用数据后需重新允许。
5. 撤销后，正在运行的官方 APK 通常在下一次心跳（约一分钟，加上网络响应时间）停止。设备长时间离线不能继续验证。

源管理、原始 M3U 导出和旧兼容导出接口现在需要管理员登录；设备下发与点播使用设备密钥。带私有 KEY 的订阅仍是独立访问凭证，需要停用对应套餐或刷新 KEY 才能撤销。

接口有按连接 IP 的限流和待授权数量上限。反代默认不信任传入的 X-Forwarded-For，因此反代后的设备可能共享额度。此实现**不代理视频流，也不限制直播源自身的并发连接**；已泄露的源直链需要在源服务、防火墙或网关限制。公网设备连接建议使用 HTTPS。

## 节目单

支持 XMLTV / gzip、频道 display-name 和 tvg-name 匹配；下发当前和接下来两天内的节目，过滤过期条目。源同步改变频道 ID 后按保留的频道名继续关联。APK 显示现在/下个节目、播出时间和进度，每半分钟更新进度，每分钟刷新数据。

进度是节目播出时间比例，不代表直播支持快进或回看。后台先添加并同步可用 XMLTV 源，检查“有效节目”数量；为 0 时核对源内容、节目日期与频道名称。

## 点播

在 **家庭点播** 添加自己的苹果 CMS JSON 接口（返回 `list`，支持 `ac=detail`、`wd`、`pg`、`ids`）。APK 顶部点播入口提供换源、搜索、翻页、线路和集数选择。播放器支持暂停和进度拖动。

只处理接口中的 HTTP/HTTPS 视频地址，不提供影片资源、不处理网页解析或 DRM。源返回网页而非媒体时可能无法播放。未配置时 APK 显示明确指引。

## 验证范围

后端测试覆盖授权绕过、错误密钥、过期/撤销/停用、EPG 别名与时间窗口、频道重新编号、点播解析及本地 TXT/M3U 导入。Android 自动化覆盖 Android 9/15 启动、设备身份保存、手机导航/EPG 进度/旋转以及电视初始焦点；另运行 lint 与签名构建。实际极光 5C、当贝 H3 仍需实机验证遥控器与源的音视频解码。
